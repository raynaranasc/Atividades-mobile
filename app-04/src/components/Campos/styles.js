import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  input:{
    marginTop: 20,
    height: 45,
    borderWidth: 1,
    borderColor: '#222',
    marginStart: 15,
    marginEnd: 15,
    fontSize: 20,
    padding: 10,
  },
})

export {styles}