import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  titulo:{
    fontSize: 30,
    fontWeight: 'bold',
    color: 'blue',
    textAlign: 'center'
  },

  tela:{
    marginTop: 30
  },
})

export {styles}